.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
models/
.claude/

# Retrieval index. Rebuildable from the source documents with scripts/ingest.py, and
# it is a binary sqlite file, so committing it would only produce unreviewable diffs.
data/

# Generated by deploy/searxng/up.sh from settings.yml.example, with a secret key
# minted locally. The template is committed; the instance file is not.
deploy/searxng/settings.yml
